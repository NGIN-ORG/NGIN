# NGIN.UI Accessibility for Windows

This package exposes NGIN.UI semantic snapshots through Windows UI Automation.
It supports native roles, names, values, focus, selection, ranges, actions, and
change events. On non-Windows systems the factory returns no provider, allowing
the same application source to keep an honest unsupported status.

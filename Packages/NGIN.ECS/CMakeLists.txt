if(TARGET NGIN::ECS)
  return()
endif()

set(_ngin_ecs_source_dir "${CMAKE_CURRENT_LIST_DIR}/../../Dependencies/NGIN/NGIN.ECS")
set(_ngin_base_source_dir "${CMAKE_CURRENT_LIST_DIR}/../../Dependencies/NGIN/NGIN.Base")
if(NOT EXISTS "${_ngin_ecs_source_dir}/CMakeLists.txt")
  message(FATAL_ERROR "Packages/NGIN.ECS requires Dependencies/NGIN/NGIN.ECS")
endif()
if(NOT EXISTS "${_ngin_base_source_dir}/CMakeLists.txt")
  message(FATAL_ERROR "Packages/NGIN.ECS requires Dependencies/NGIN/NGIN.Base")
endif()

set(NGIN_BASE_SOURCE_DIR "${_ngin_base_source_dir}" CACHE PATH "" FORCE)
set(NGIN_ECS_BUILD_TESTS OFF CACHE BOOL "" FORCE)
set(NGIN_ECS_BUILD_EXAMPLES OFF CACHE BOOL "" FORCE)
add_subdirectory("${_ngin_ecs_source_dir}" "${CMAKE_CURRENT_BINARY_DIR}/_ngin_ecs" EXCLUDE_FROM_ALL)

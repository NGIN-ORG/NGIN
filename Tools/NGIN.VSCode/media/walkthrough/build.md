# Stay in the editor loop

Click a project to select it, then use Configure, Build, Run, or Debug in the status bar. F5 and file-specific commands use the project owning the active file, with the selected project as their fallback.

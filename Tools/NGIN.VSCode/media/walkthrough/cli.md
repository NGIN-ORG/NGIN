# Use the same NGIN CLI everywhere

The extension calls the native `ngin` CLI for project discovery, validation, composition, builds, and launch intent. Configure an explicit executable only when the development build and `PATH` lookup are not suitable.

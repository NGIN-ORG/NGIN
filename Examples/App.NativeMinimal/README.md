# App.NativeMinimal

`App.NativeMinimal` is the smallest plain native application example.

It demonstrates that NGIN tooling can validate, build, stage, and run a C++
executable without linking `NGIN.Core` and without using the hosted application
runtime.

The application is just normal C++ source plus an `.nginproj` file.

